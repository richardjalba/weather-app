const lodash = require("lodash");
const express = require("express");

const app = express();
const PORT = "4000";

app.listen(PORT, () => {
  console.logt(`server sunning at por0t : ${PORT}`);
});
app.listen("");
